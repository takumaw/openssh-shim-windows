# HISTORY

## 0.1.1

  * Add Windows version resource files for `ssh.exe` and `scp.exe`.
  * Enable MSVC compile options `/sdl` and `/analyze` for build hardening.

## 0.1.0

  * Initial release with `ssh.exe` and `scp.exe` shims for Windows OpenSSH.
